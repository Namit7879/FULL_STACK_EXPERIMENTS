# Exp-5

React-based experiments for routing and context.

## Structure

- 5.1/ - React Context API experiment.
- 5.2/ - React Router SPA with lazy loading and styling.

## Usage

For each React app, install dependencies and run the dev server:

- `npm install`
- `npm run dev`
